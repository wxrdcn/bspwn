# bspwn
dotfiles configuration for kali-linux

```bash
sudo bash install.sh
```
autocolor.sh
can change a bit of the colorscheme

```bash
$ bash autocolor.sh
[+] Usage: autocolor <first_color> <second_color>
	Example: autocolor "#FF0000" "#00FF00"
```
